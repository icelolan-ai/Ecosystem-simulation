# CLAUDE.md — บันทึกสำหรับคนที่มาทำงานต่อ

โปรเจกต์นี้คือ **ระบบนิเวศจำลอง 3 มิติในภาชนะแก้ว** (Three.js + ES modules ล้วน ไม่มี build step)
ไฟล์นี้เก็บสิ่งที่ต้องรู้ก่อนแก้โค้ด โดยเฉพาะเรื่องที่เคยทำให้พลาดมาแล้ว

## กฎข้อเดียวที่สำคัญที่สุด

```bash
node tools/balance-test.mjs
```

**รันทุกครั้งหลังแก้อะไรก็ตามใน `js/config.js` หรือ `js/simulation.js`**

ค่าสมดุลของระบบนี้เปราะมาก การขยับตัวเลขเดียวทำให้ทั้งระบบล่มได้เงียบ ๆ
เทสต์ใช้เวลา ~25 วินาที และ exit 1 เมื่อไม่ผ่าน

> ประวัติ: เทสต์รุ่นแรกพิมพ์ผลอย่างเดียวแล้ว exit 0 เสมอ CI จึงเขียวตลอดแม้ระบบจะพัง
> ถ้าเพิ่มการตรวจใหม่ ให้แน่ใจว่ามันทำให้ exit code เปลี่ยนจริง — ลองจงใจทำพังแล้วดูว่าจับได้

## สถาปัตยกรรม

```
js/simulation.js   แกนระบบนิเวศ — ไม่ import DOM หรือ Three.js เลย (ตั้งใจให้เป็นแบบนี้)
js/config.js       ค่าคงที่ พารามิเตอร์ และ preset ทั้งหมด
js/rng.js          mulberry32 — ค่าสุ่มทุกค่าในซิมูเลชันต้องมาจากตัวนี้
js/scene.js        Three.js — วาดสถานะอย่างเดียว ไม่มีตรรกะนิเวศ
js/chart.js        กราฟ Canvas 2D
js/ui.js           แผง DOM
js/share.js        เข้ารหัส/ถอดรหัสการทดลองลง URL
js/main.js         ลูป fixed timestep + อินพุต
```

**ห้าม** ให้ `simulation.js` พึ่ง DOM หรือ Three.js — ความสามารถในการรันทดสอบแบบ headless
คือสิ่งที่ทำให้จูนระบบนี้ได้เลย

**ห้าม** ใช้ `Math.random()` ในซิมูเลชัน ใช้ `this.rng()` เท่านั้น ไม่งั้น determinism พัง
และฟีเจอร์แชร์ลิงก์จะใช้ไม่ได้

## สิ่งที่เคยเข้าใจผิดมาแล้ว (อย่าพลาดซ้ำ)

### 1. เพดานจำนวนไม่ใช่แค่ตัวคุม performance

เอกสารรุ่นแรกเขียนว่า `CAPS` มีไว้คุมต้นทุนการเรนเดอร์เท่านั้น **ไม่จริง**
ทดลองยกเพดานขึ้นแล้วอัตราการอยู่รอดตกลง เพราะประชากรบูมแรงขึ้นแล้วล่มหนักกว่าเดิม
เพดานทำหน้าที่หน่วงการบูมอยู่จริง ถ้าจะแก้ ต้องวัดผลไม่ใช่เดา

### 2. ฟีเจอร์อาจกลายเป็นของประดับโดยไม่รู้ตัว

ตอนเพิ่มธาตุอาหาร ค่าแรกที่ตั้งทำให้ตัวคูณการเติบโตเท่ากับ 1.000 ทุกจุดตลอดเวลา —
ระบบทั้งระบบไม่มีผลอะไรเลย ตอนนี้เทสต์มีข้อ `decomposers` คอยกัน
**ถ้าเพิ่มกลไกใหม่ ให้วัดว่ามันมีผลจริงไหม ไม่ใช่แค่ว่าโค้ดรันผ่าน**

### 3. ตัวเลขจาก seed น้อย ๆ หลอกได้

เคยรายงานว่า "7 จาก 8 seed อยู่รอด" แล้วพบภายหลังว่าค่าจริงราว 50%
เพราะ 8 seed นั้นบังเอิญดี **วัดด้วย seed กลางอย่างน้อย 24 ตัว** และถ้าจูนด้วย seed ชุดไหน
ให้ยืนยันผลด้วย seed อีกชุดที่ไม่เคยใช้จูน (เคยเจอกรณีที่ 16/24 กลายเป็น 21/48 บนชุดใหม่)

### 4. การแก้ที่กระทบลำดับสุ่มจะสับ seed ใหม่ทั้งหมด

เพิ่มกลไกที่เรียก `rng()` เพิ่ม (เช่น สปอร์เห็ดรา หรือการสืบทอดยีน) = seed เดิมกลายเป็นโลกคนละใบ
**ต้องเลือก seed ของ preset ใหม่ทุกครั้ง** ไม่งั้น seed หลักอาจกลายเป็นตัวที่ล่มใน 5 นาที
(เกิดมาแล้วสองรอบ) เทสต์มีข้อ `preset seed` คอยจับ

### 5. ฤดูแล้งอย่างเดียวคือยาพิษ

ตอนเพิ่มฤดูกาล ถ้ามีแต่ช่วงแห้งโดยไม่มีฝนชดเชย อัตราการอยู่รอดตกจาก 54% เหลือ 20%
พอเพิ่มฝนปรอยตามฤดูกลับดีดขึ้นเป็น 75% — ฤดูกาลที่ดีคือการ**กระจายน้ำใหม่ตามเวลา**
ไม่ใช่การหักน้ำออกจากระบบ

## กลไกที่พยุงระบบไว้ (อย่าถอดออกโดยไม่วัดผล)

1. **วัฏจักรน้ำของภาชนะปิด** — ไอน้ำกลั่นที่ผนังแล้วตกกลับ
2. **ฝนปรอยตามฤดูกาล** — ตัวพยุงที่แรงที่สุด (+21 จุดเปอร์เซ็นต์)
3. **สัตว์เล็มพืชไม่ถอนราก** — พืชงอกกลับได้
4. **เหยื่อที่ตื่นตัววิ่งเร็วกว่าผู้ล่า** — ส่วนต่างบางมาก (2.56 vs 2.38) ยีนขยับนิดเดียวก็พลิกผล
5. **พุ่มไม้เป็นที่กำบัง** — เหยื่อน้อย → พืชฟื้น → กำบังดี → เหยื่อรอด
6. **ผู้ล่าหวงอาณาเขต** — คุมจำนวนผู้ล่าด้วยพื้นที่
7. **มูลสัตว์คืนสารอาหาร** — ถ้าไม่มี ดินจะจืดลงเรื่อย ๆ เพราะชีวมวลที่ถูกกินหายออกจากระบบ

## การจูน

`tools/balance-test.mjs --trace` พิมพ์เส้นทางประชากรของทุก preset
สคริปต์สำรวจแบบใช้แล้วทิ้งเขียนไว้ที่ scratchpad ได้ รูปแบบที่ใช้ได้ผลคือ:

```js
import { Ecosystem } from './js/simulation.js';
import { PREDATOR } from './js/config.js';   // แก้ค่าใน memory แล้ววัดผลเป็น matrix
```

เพราะ config เป็น object ธรรมดา การ sweep จึงทำได้โดยไม่ต้องแก้ไฟล์

## CI / Deploy

`.github/workflows/pages.yml` — รันเทสต์ทุก push และทุก PR แล้ว deploy ขึ้น GitHub Pages
เมื่อเข้า `main` (Pages Source ตั้งเป็น "GitHub Actions")

Three.js ถูก vendor ไว้ใน `js/vendor/` **อย่าเปลี่ยนไปใช้ CDN** — เคยลองแล้วถูก network policy บล็อก
และการ vendor ทำให้เปิดใช้ได้แบบ offline โดยไม่ต้อง build
